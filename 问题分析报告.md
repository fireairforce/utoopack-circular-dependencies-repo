# 问题分析报告：@antv/g-canvas Shape 导入导致的运行时错误

## 错误现象

运行时抛出错误：
```
Uncaught TypeError: Class extends value undefined is not a constructor or null
```

错误发生在 `__extends` 函数中，当某个类尝试继承一个 `undefined` 的值时。

## 根本原因：循环依赖

### 依赖关系链

1. **用户代码**：
   ```typescript
   import { Shape } from '@antv/g-canvas';
   Shape;  // 即使只是访问，也会触发模块加载
   ```

2. **@antv/g-canvas 的导出结构**：
   - `esm/index.js` 导出：`export { Shape }`，其中 `Shape` 来自 `import * as Shape from './shape'`
   - `esm/shape/index.js` 导出所有 shape 类：`export { default as Base } from './base'` 等

3. **循环依赖链**：
   ```
   shape/index.js
     ↓ (导出 Base)
   shape/base.js
     ↓ (第6行: import * as Shape from './index')
   shape/index.js  ← 循环依赖！
   ```

### 具体问题

在 `shape/base.js` 的第6行：
```javascript
import * as Shape from './index';
```

这创建了一个循环依赖：
- `shape/index.js` 需要导出 `Base`（来自 `base.js`）
- `base.js` 需要导入 `shape/index.js` 作为 `Shape`
- 当其他 shape 类（如 `Path`）继承 `ShapeBase` 时，由于循环依赖，`ShapeBase` 可能还未完全初始化，导致它是 `undefined`

### 为什么 `Shape;` 就会触发问题？

即使代码只是访问 `Shape` 而不使用，打包工具（utoopack/turbopack）也会：
1. 强制加载整个 `Shape` 命名空间对象
2. 触发 `shape/index.js` 的加载
3. 加载所有 shape 类（Base, Circle, Path 等）
4. 触发循环依赖链的加载
5. 在循环依赖解析过程中，某些类在继承时遇到 `undefined` 的基类

## 构建产物分析

在 `dist/src_9157d213.js` 中可以看到：
```javascript
// 第15行：导入 shape/index.js
var __TURBOPACK__imported__module__...shape$2f$index$2e$js... = 
  __turbopack_context__.i("...shape/index.js...");

// 第19行：访问 Shape（触发加载）
__TURBOPACK__imported__module__...["Shape"];
```

在 `dist/node_modules__pnpm_acba727e.js` 中：
- 第5524-5525行：`ShapeBase` 的定义和继承
- 第6487行：`Path` 类尝试继承 `ShapeBase`，但此时 `_super` 可能是 `undefined`

## 解决方案

### 方案1：移除未使用的导入（推荐）

如果不需要使用 `Shape`，直接删除这行代码：

```typescript
// 删除这两行
// import { Shape } from '@antv/g-canvas';
// Shape;
```

### 方案2：按需导入特定类

如果确实需要使用 shape 类，不要导入整个 `Shape` 命名空间，而是按需导入：

```typescript
// 只导入需要的类
import { Base, Circle } from '@antv/g-canvas';
// 或者
import { Base } from '@antv/g-canvas/esm/shape/base';
```

### 方案3：延迟加载

如果必须使用 `Shape` 命名空间，可以延迟加载：

```typescript
// 在需要时才加载
const loadShape = async () => {
  const { Shape } = await import('@antv/g-canvas');
  return Shape;
};
```

## 总结

这是一个典型的**循环依赖**问题，由 `@antv/g-canvas` 库的内部设计导致。当打包工具（utoopack）处理这种循环依赖时，在某些情况下无法正确解析模块的初始化顺序，导致基类在子类继承时还是 `undefined`。

最简单的解决方案是：**如果不需要使用 `Shape`，直接删除导入语句**。
